# variable is the one who can change itself
# we store variable in Ram, and program run in Ram (Random Access Memory)
# Ram has a memory adress 

# length = 100
# #print(id(length)) #id()to get memory adress
# breadth = 100

# print(id(length),id(breadth))

# same value will have same id output eventhough it has diffrent variable name

length_of_land = 100
breadth_of_land = 100
bricks_cost_per_piece = 10.5
labour_mistiri1 = 'Jagmohan'
labour_mistiri2 = 'Rampyare'
is_home = True

print(length_of_land,bricks_cost_per_piece,labour_mistiri1,is_home)
print(type(length_of_land),type(bricks_cost_per_piece),type(labour_mistiri1),type(is_home))